export interface Task {
  id: string; // Não está retornando o id no backend
  name: string;
  email: string;
  registration: string;
  password: string;
  role: string;
  createdAt: string;
  updatedAt: string;
}
